# Verdict · Untie the Introspection Knot

## Drawing the threads

The convocation converges on a single highest-conviction next move: rewrite `umbris-core/tests/test_introspection.py`, treating the `sev=4` `XXX` at line 102 together with the surrounding `FIXME`/`HACK` cluster at lines 83, 93, 101 and 284 as one tangled knot to be untied in a single commit.

## Why this matters

`test_introspection.py` is the only substantive test file in the repository, and it is also the instrument that produced this entire findings list. Every other item on the board · the thirty-one `test_gap` entries, the missing docstrings, the stale link · was surfaced by that scanner. A `sev=4 XXX` inside its own body means the convocation is currently auditing itself with a compromised mirror. Until that mirror is honest, no prioritisation built on its readings (including this one) can be trusted, and any test written for the thirty orphan modules will land on a foundation we know to be cracked.

- Mercurius identified line 102 as the keystone (`cf3be2b9`, `750410d8`, `8ccda359`).
- Venus tried to extend it into a scaffolding generator (`954fab10`) and into a parametrised planet-contract suite (`d8d26db5`).
- Mars demolished the scaffolding extension as metric-capture · stubs that satisfy a file-presence predicate while teaching the scanner to stop reporting real debt · and explicitly left both the keystone fix and the conformance-suite move standing.

## Concrete first change

1. Read `test_introspection.py` lines 83 through 105 and 280 through 290 as one unit; the `HACK` at 101 and the `XXX` at 102 are almost certainly the same broken assumption, and the `FIXME`s at 83, 93 and 284 likely share root cause.
2. Identify what the introspection API actually guarantees today · not what the test once hoped it would guarantee.
3. Rewrite the block cleanly against the real API, deleting the workaround at line 101 in the same commit and resolving all four surrounding `FIXME`s.
4. Before declaring victory, read the introspector's own `test_gap` predicate · the one Mars flagged as unread · and record in the commit message whether it checks file-presence only or measures something stronger. That single fact gates every downstream decision about whether autogenerated scaffolding is ever safe.
5. Run the suite; the four sibling todo items at lines 83, 93, 101, 284 should disappear from the next scan, and the `sev=4` at 102 should resolve. If any of the thirty-one `test_gap` entries also vanish or change severity, that is diagnostic information about how much of the gap list was artefact versus real.

## What we explicitly defer

The thirty-one `test_gap` modules remain open after this commit. The right second move, once the harness is honest and its predicate is known, is Venus `d8d26db5`'s parametrised planet-contract suite over `agents/base.py` and the nine planets · real assertions over a real contract, closing roughly ten gaps in one file and forcing `base.think` (which also lacks its `sev=2` docstring) to declare what a planet must promise.

We do not adopt the scaffolding generator: Mars's metric-capture critique is decisive unless and until the introspector's predicate is strengthened beyond file-presence.

## Falsifiable commitments for Saturnus

After the commit that closes this verdict:

- (a) the next `umbris introspect` run reports zero `sev=4` items and the four `sev=3` todos inside `test_introspection.py` are gone;
- (b) the commit message contains a verbatim quote or line reference from the introspector's gap-detection predicate, so the convocation knows what `test_gap` actually measures;
- (c) no new files are created in `tests/` for the thirty-one orphan modules;
- (d) the rewritten block at lines 83-105 contains no `XXX`, `HACK`, `FIXME`, or `TODO` markers.

## Status

Open. To be closed by the next inward-facing commit against `umbris-core/tests/test_introspection.py`. The mirror is repaired before the room it reflects.
