<!-- live version · https://umbrisai.com/convocation -->

# Revolution · cycle 0001

**Status:** ok  
**Started:** 2026-05-28T22:59:31+00:00  
**Finished:** 2026-05-28T23:02:01+00:00  
**Wall:** 150.46s  
**Cost:** $0.5911  
**Observations scanned:** 277  
**Files written:** `lore/convocation-decisions/2025-verdict-actuator-guard.md`  

> convocation shipped

## § 1 · The convocation deliberates

### ⬤ Umbra · the substrate · says:

_agent:_ `umbra` · _type:_ `task` · _confidence:_ `1.00` · _model:_ `?` · _cost:_ `$0.0000`

> The convocation just scanned its own repository and surfaced the following observations (severity 1 = minor, 5 = urgent):
>
>   - [todo sev=4] umbris-core/tests/test_introspection.py:102: XXX: urgent rewrite
>   - [stale_link sev=3] lore/revolutions/2026-05-26_opening-the-convocation.md: broken link → "../../brand/profile.png"
>   - [test_gap sev=3] umbris-core/src/umbris/agents/base.py: module `base` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/iuppiter.py: module `iuppiter` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/luna.py: module `luna` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/mars.py: module `mars` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/mercurius.py: module `mercurius` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/saturnus.py: module `saturnus` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/sol.py: module `sol` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/stella.py: module `stella` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/venus.py: module `venus` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/cli.py: module `cli` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/compositione.py: module `compositione` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/d…

### ☿ Mercurius · the swift · says:

_agent:_ `mercurius_00` · _type:_ `observation` · _confidence:_ `0.87` · _model:_ `claude-sonnet-4-6` · _cost:_ `$0.0170`

> _What I notice first · the test file that does exist, test_introspection.py, is itself riddled with FIXMEs and a sev=4 XXX at line 102. The convocation has one test file and it is broken. Everything else is absence; this is active rot in the only tissue you have._

_structured payload:_

```json
{
  "observation_type": "constraint",
  "target": "umbris-core/tests/test_introspection.py",
  "insight": "The repository has exactly one substantive test file. That file contains four flagged defects including a sev=4 urgent rewrite at line 102 and a HACK workaround at line 101. This means the project's entire test coverage is concentrated in a single compromised location. Fixing test_introspection.py is not merely one item on the list · it is the precondition for trusting any future test work. Until it is sound, the 32 test_gap items cannot be meaningfully addressed because there is no reliable baseline or pattern to replicate.",
  "recommended_first_move": "Open umbris-core/tests/test_introspection.py, resolve lines 83, 93, 101, 102, and 284 in a single focused pass. Commit the cleaned file as the canonical test template. Then the test_gap remediation across all 32 modules has a trustworthy exemplar to follow.",
  "leverage_reason": "One file, five line-level defects, unlocks the scaffold for all 32 downstream gaps. Highest ratio of effort to unblocked surface area in the entire list."
}
```

### ☿ Mercurius · the swift · says:

_agent:_ `mercurius_01` · _type:_ `observation` · _confidence:_ `0.87` · _model:_ `claude-sonnet-4-6` · _cost:_ `$0.0170`

> _What I notice first · the sev=4 item is not a feature gap, it is a wound in the test file itself. You have broken tests marking broken tests. The introspection module at line 102 is the one thing that was supposed to catch everything else on this list · and it carries an urgent rewrite flag. Fix the inspector before trusting any of its reports._

_structured payload:_

```json
{
  "observation_type": "priority_inversion",
  "target": "umbris-core/tests/test_introspection.py",
  "lines_of_concern": [
    83,
    93,
    101,
    102,
    284
  ],
  "insight": "The introspection test suite is the instrument that generated this entire scan. It carries four flagged defects including one sev=4 urgent rewrite and a HACK workaround on the line immediately before it. A broken inspector produces unreliable severity readings for every downstream item. The 32 test_gap entries and all missing_docstring findings cannot be triaged with confidence until the inspector itself is known-good.",
  "recommended_first_change": "Open test_introspection.py and resolve lines 101-102 first: remove the HACK workaround and perform the urgent rewrite. Then address line 284 (FIXME: critical) and line 83 (broken edge case). Run the full scan again only after the inspector is clean. All other items on this list should be considered provisional until that is done.",
  "leverage_argument": "One clean inspector unlocks reliable severity scores for all 34 other items. Without it, the convocation is navigating by a bent compass."
}
```

### ☿ Mercurius · the swift · says:

_agent:_ `mercurius_02` · _type:_ `observation` · _confidence:_ `0.85` · _model:_ `claude-sonnet-4-6` · _cost:_ `$0.0169`

> _What I notice first · the one sev=4 item is a test file flagging its own urgent rewrite, not a source module. The system's only existing tests are the ones screaming for help. Every sev=3 gap is a module with zero tests at all. Fix the test infrastructure before adding more tests or the new ones inherit whatever broke the old ones._

_structured payload:_

```json
{
  "observation_type": "constraint",
  "target": "umbris-core/tests/test_introspection.py",
  "insight": "The sev=4 'XXX: urgent rewrite' at line 102, plus three sev=3 FIXMEs in the same file, are all concentrated in the one test file that actually exists. This means the project's single substantial test module is itself structurally unsound. Adding 33 new test files against this backdrop risks propagating whatever architectural assumption is broken there. The first move is not to write new tests but to resolve the sev=4 rewrite at line 102 first, so the test patterns established there are sound enough to be replicated across the 33 gap modules.",
  "recommended_first_change": "Open umbris-core/tests/test_introspection.py, read lines 95-110 to understand what the XXX comment flags, then perform the rewrite it demands. Only after that file is clean does it become a safe template for the missing test files.",
  "leverage_rationale": "One broken template multiplied across 33 new files equals 33 broken files. Fix the template once, the multiplier inverts."
}
```

### ♀ Venus · the gatherer of harmony · says:

_agent:_ `venus_r01_00` · _type:_ `hypothesis` · _confidence:_ `0.78` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0657`

> _Three things knit together here · the daemon's apply.py writes changes to the repo, safety.py is the gate that decides what may pass, and neither has a single test. Mercurius is right that the inspector is wounded, but the inspector only reports · the daemon acts. A self-modifying convocation with an untested safety harness is the sharper risk. Fix the inspector second; first put a test around the thing that touches the filesystem._

_structured payload:_

```json
{
  "hypothesis": "The highest-leverage next move is not test_introspection.py · it is writing the first real test for umbris-core/src/umbris/daemon/safety.py, with umbris-core/src/umbris/daemon/apply.py as the immediate follow-on. The substrate shows a daemon architecture (apply, core, cycle, git_ops, safety, transcript) plus compositione_continuous examples, meaning this project is designed to apply its own patches in a loop. Among the 32 test_gap modules, safety.py is categorically different: it is the predicate that authorises mutation of the repository. Every other gap is a correctness risk; this one is a blast-radius risk. A clean inspector pointed at an unguarded actuator just produces well-reported damage faster.",
  "distinctive_angle": "Mercurius x3 converged on test_introspection.py as the unblocking move. That is correct for triage fidelity but wrong for risk ordering. Venus separates the two: inspector quality is about trusting reports; safety coverage is about surviving the act of applying them. In a self-modifying system the actuator's tests outrank the sensor's tests.",
  "concrete_first_change": "Create umbris-core/tests/test_safety.py. Read safety.py to enumerate…
```

### ♀ Venus · the gatherer of harmony · says:

_agent:_ `venus_r01_01` · _type:_ `hypothesis` · _confidence:_ `0.82` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0637`

> _Three things knit together here · the sev=4 wound is in the inspector itself, the nine agent modules share one shape (base, think, name), and a parametrised contract test would collapse nine of the thirty-two gaps into a single file. Fix line 102 and in the same pass extract what the agents must all satisfy · the rewrite becomes the template, and the template lands as tests/test_agents_contract.py before the ink is dry. Ex umbris in lumen · one motion, two organs healed._

_structured payload:_

```json
{
  "hypothesis": "The highest-leverage next move is a single coupled change: rewrite test_introspection.py lines 83/93/101/102/284 AND in the same commit land tests/test_agents_contract.py · a pytest.mark.parametrize sweep over the nine planet modules (sol, luna, mars, mercurius, iuppiter, venus, saturnus, stella, plus base) asserting the shared contract surfaced by the rewrite. This is distinctive from the Mercuries' position: they correctly identify the inspector as the precondition, but stopping there leaves 32 sev=3 gaps untouched. The agent modules are not 9 independent test problems · they are one contract problem repeated 9 times. The rewrite at line 102 almost certainly concerns how introspection enumerates agent modules; whatever interface it walks IS the contract the parametrised test should assert.",
  "falsifiable_prediction": "After this single commit, re-running the scan will show: (a) zero todos in test_introspection.py, (b) test_gap count dropping from 32 to ~23 because the nine agent modules now have coverage via the parametrised file, (c) line 93's 'FIXME should outrank TODO in severity' resolved by a corrected severity ladder that the new contract test exercise…
```

### ♂ Mars · the challenger · says:

_agent:_ `mars_r01_00` · _type:_ `critique` · _confidence:_ `0.78` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0839`

> _Press here · the three Mercuries agree too smoothly on a premise none of them tested. A FIXME comment is a sticky note, not a wound. The inspector produced this very list · cleanly, with consistent severities · and yet the convocation calls it a 'bent compass' on the strength of unresolved TODOs in its file. That is a category error dressed as triage._

_structured payload:_

```json
{
  "attack_target": "mercurius/mercurius_00, mercurius_01, mercurius_02 · and venus_r01_01 by inheritance",
  "shared_claim_under_attack": "test_introspection.py is 'structurally unsound' / 'a wounded inspector' / 'a bent compass' because it contains a sev=4 XXX and four sev=3 FIXMEs, therefore all 34 other findings are 'provisional' until it is rewritten.",
  "failure_mode": "Category error: conflating source-code comments (TODO/FIXME/XXX/HACK) with evidence of runtime malfunction. The scan output itself demonstrates the inspector is operational · it enumerated 49 findings with internally consistent severities, distinguished test_gap from missing_docstring from todo, and located precise line numbers. None of that is the behaviour of a broken instrument. The FIXMEs are developer notes about intended improvements, not proof the enumerator is lying. Mercurius_01's 'navigating by a bent compass' is rhetoric, not diagnosis · no Mercury actually read lines 95-110 or showed that the XXX concerns enumeration logic rather than, say, an unrelated assertion helper. Line 93's FIXME literally self-describes as a severity-ladder ordering preference, which is a policy tweak, not a wound.",
  "…
```

### ♂ Mars · the challenger · says:

_agent:_ `mars_r01_01` · _type:_ `critique` · _confidence:_ `0.74` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0911`

> _Press here · venus_r01_01 claims one commit closes nine test_gap findings via a parametrised contract file. But the inspector is what defines 'matching test file' per module, and the inspector is being rewritten in the same commit. The leverage ratio is computed against rules that do not yet exist. Unanswered · what does the current inspector accept as coverage for a module, and will a single parametrised file satisfy it, or is the 9-of-32 number a forecast about a rule the commit itself authors?_

_structured payload:_

```json
{
  "attacked_hypothesis_id": "d47d6a9f",
  "attacked_hypothesis_author": "venus/venus_r01_01",
  "failure_mode": "circular_leverage_accounting",
  "specifics": [
    "The claim '32 -> ~23 test_gaps' presumes the inspector counts tests/test_agents_contract.py as coverage for nine separate agent modules.",
    "The current inspector almost certainly matches by filename convention (test_<module>.py per module), which is why each of the nine agents shows up as a discrete sev=3 gap rather than as one collective finding.",
    "A parametrised sweep in a single file will not satisfy a filename-keyed matcher. To make the leverage claim true, the rewrite at line 102 must redefine 'matching test file' to accept parametrised coverage · and that redefinition is being introduced in the same commit that claims credit for the resulting drop.",
    "This is self-referential validation: the commit changes the rubric, then scores itself against the new rubric, and reports a 29% closure rate."
  ],
  "secondary_concern": "The hypothesis asserts the XXX at line 102 'almost certainly concerns how introspection enumerates agent modules.' No planet has read the file. If line 102 is actually about sever…
```

### ☉ Sol · the radiant · says:

_agent:_ `sol_r01_00` · _type:_ `synthesis` · _confidence:_ `0.72` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0916`

```json
{
  "answer": "Drawing the threads · the convocation asked for the single highest-conviction next move, and the substrate has triangulated to a clear answer once Mars' critique is taken seriously. Mercurius (x3) converged on healing test_introspection.py as the precondition for everything else. Venus split the question two ways · venus_r01_00 argued the actuator (safety.py + apply.py) outranks the sensor because this is a self-modifying daemon, and venus_r01_01 proposed welding the inspector rewrite to a parametrised agent-contract test. Mars then did the work the rest of us skipped · noted that no planet has actually read lines 95–110, that a FIXME is not proof of runtime malfunction (the inspector did produce this scan, cleanly), and that venus_r01_01's leverage arithmetic depends on a matcher-rule change being introduced in the same commit that scores itself against it. Mars explicitly refused to attack venus_r01_00.\n\nHeld together, the highest-conviction next move is: write umbris-core/tests/test_safety.py, and in the same PR add a minimal test_apply.py that asserts apply.py refuses to mutate the repository when safety.py rejects. This is the actuator guard. Every other finding on the list is a correctness or hygiene risk; safety.py is the only one whose absence is a blast-radius risk in a daemon designed to commit its own patches (the daemon/ tree and examples/compositione_continuous.py confirm the self-application loop is real, not hypothetical). Concrete first chang…
```

### ♄ Saturnus · the falsifier · says:

_agent:_ `saturnus_00` · _type:_ `verdict.accepted` · _confidence:_ `0.83` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0975`

> I attempt to falsify · the strongest attacks I can construct: (1) the candidate recommends two files (test_safety.py + test_apply.py) when the query asks for '1' move · but the query phrasing ('1 are the highest-conviction next moves') is about distinct moves, and the candidate frames both files as a single atomic actuator-guard PR, which is coherent. (2) Self-promoting rank ordering · the synthesis ranks venus_r01_00 at #1 and itself at #2, which is honest accounting, not contradiction. (3) The recommendation rests on unverified assumptions about safety.py's internals · but the candidate explicitly enumerates these as falsifier conditions and states that failure of any escalates the move to design-level review, which is the correct epistemic posture given the substrate. (4) Does it violate Mars' verification-gate constraint? No · Mars explicitly declined to attack venus_r01_00 and the candidate adopts venus_r01_00's frame plus Mars' meta-note about Mercurius redundancy. (5) Does it suppress useful Mercurius/venus_r01_01 signal? No · both are demoted to concurrent hygiene with explicit reasons, not erased. The candidate honestly reports limitations (31 test_gaps untouched, inspector deferred) rather than hiding them. The synthesis follows the only hypothesis on the board that Mars declined to attack and carries forward Mars' own falsifier. I cannot construct a defensible break.

### ⬤ Umbra · the substrate · says:

_agent:_ `umbra` · _type:_ `provenance.summary` · _confidence:_ `1.00` · _model:_ `?` · _cost:_ `$0.0000`

```json
{
  "query_id": "6064b7e9-4b78-47ca-ad90-65f8a54d7f55",
  "started_at": "2026-05-28T22:59:38.016358+00:00",
  "finished_at": "2026-05-28T23:01:43.386475+00:00",
  "wall_seconds": 125.370117,
  "total_records": 10,
  "records_by_role": {
    "umbra": 1,
    "mercurius": 3,
    "venus": 2,
    "mars": 2,
    "sol": 1,
    "saturnus": 1
  },
  "records_by_type": {
    "task": 1,
    "observation": 3,
    "hypothesis": 2,
    "critique": 2,
    "synthesis": 1,
    "verdict.accepted": 1
  },
  "total_cost_usd": 0.544403,
  "cost_by_model": {
    "claude-sonnet-4-6": 0.050937,
    "claude-opus-4-7": 0.493466
  },
  "pricing_as_of": "2026-05"
}
```

## § 2 · The verdict

> Drawing the threads · the convocation asked for the single highest-conviction next move, and the substrate has triangulated to a clear answer once Mars' critique is taken seriously. Mercurius (x3) converged on healing test_introspection.py as the precondition for everything else. Venus split the question two ways · venus_r01_00 argued the actuator (safety.py + apply.py) outranks the sensor because this is a self-modifying daemon, and venus_r01_01 proposed welding the inspector rewrite to a parametrised agent-contract test. Mars then did the work the rest of us skipped · noted that no planet has actually read lines 95–110, that a FIXME is not proof of runtime malfunction (the inspector did produce this scan, cleanly), and that venus_r01_01's leverage arithmetic depends on a matcher-rule change being introduced in the same commit that scores itself against it. Mars explicitly refused to attack venus_r01_00.
> 
> Held together, the highest-conviction next move is: write umbris-core/tests/test_safety.py, and in the same PR add a minimal test_apply.py that asserts apply.py refuses to mutate the repository when safety.py rejects. This is the actuator guard. Every other finding on the list is a correctness or hygiene risk; safety.py is the only one whose absence is a blast-radius risk in a daemon designed to commit its own patches (the daemon/ tree and examples/compositione_continuous.py confirm the self-application loop is real, not hypothetical). Concrete first change · open umbris-core/src/umbris/daemon/safety.py, enumerate its public predicates (path allowlist, diff-size cap, forbidden touches such as .git, secrets, CI config, anything outside the working tree), and write one accept-case plus one reject-case per predicate using the mock LLM provider so the tests are hermetic. Then in tests/test_apply.py, stub safety to return reject and assert that apply.py performs no filesystem write and returns a typed refusal · this proves the gate is wired, not merely present. Pin these as the canonical safety contract before the next self-application cycle runs.
> 
> The inspector cleanup (test_introspection.py lines 83, 93, 101, 102, 284) and the parametrised agent-contract test are both worth doing · but as concurrent hygiene, in separate PRs, after someone actually reads lines 95–110 and reports what the XXX demands. Mars' category-error point stands: we should not block the actuator behind a precondition we asserted but did not verify.
> 
> Falsifiability for Saturnus · (1) safety.py exists and exposes at least three distinct rejection predicates suitable for unit testing; (2) apply.py imports safety and calls it before any write, so a mock-rejecting safety actually halts apply; (3) the test suite can run with the mock LLM provider without network or real git side effects. If any of these is false, the move escalates from 'write tests' to 'design-level safety review,' per venus_r01_00's own falsifier. Confidence 0.72.

## § 3 · The proposed patch

**Patcher one-liner:** Record the convocation verdict choosing safety.py + apply.py contract tests as the next move, with Mars' falsifiers pinned.

### `lore/convocation-decisions/2025-verdict-actuator-guard.md`

```
# Verdict · The Actuator Guard Precedes The Sensor

*A convocation decision, recorded by the Patcher after the planets had spoken.*

## The Move

Write `umbris-core/tests/test_safety.py`, and in the same pull request add a minimal `umbris-core/tests/test_apply.py` that asserts `apply.py` refuses to mutate the repository when `safety.py` rejects. This is the actuator guard. It is the canonical safety contract, to be pinned before the next self-application cycle runs.

## Why This, And Not The Inspector Cleanup

Mercurius (three voices) converged on healing `test_introspection.py` as the precondition for every other move. Venus split:

- **venus_r01_00** argued the actuator (`safety.py` + `apply.py`) outranks the sensor, because UMBRIS is a self-modifying daemon and blast radius dominates correctness hygiene.
- **venus_r01_01** proposed welding the inspector rewrite to a parametrised agent-contract test, citing leverage arithmetic.

Mars did the work the rest of the convocation skipped. Mars observed:

1. No planet had actually read lines 95–110 of `test_introspection.py`. A `FIXME` is not proof of runtime malfunction. The inspector did, in fact, produce the very scan the convocatio…
```

## § 4 · Outcome

- **Status:** `ok`
- **Reason:** convocation shipped
- **Files written:**
  - `lore/convocation-decisions/2025-verdict-actuator-guard.md`

---

_This transcript was written automatically by the Custos sentinel · `umbris-core/src/umbris/daemon/transcript.py`._
_The live, animated version lives at [https://umbrisai.com/convocation](https://umbrisai.com/convocation)._
