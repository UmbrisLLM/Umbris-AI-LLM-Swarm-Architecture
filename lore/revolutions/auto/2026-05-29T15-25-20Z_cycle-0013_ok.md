<!-- live version · https://umbrisai.com/convocation -->

# Revolution · cycle 0013

**Status:** ok  
**Started:** 2026-05-29T15:25:20+00:00  
**Finished:** 2026-05-29T15:27:49+00:00  
**Wall:** 149.12s  
**Cost:** $0.5693  
**Observations scanned:** 277  
**Files written:** `lore/convocation-decisions/0007-read-before-repair-test-introspection.md`  

> convocation shipped

## § 1 · The convocation deliberates

### ⬤ Umbra · the substrate · says:

_agent:_ `umbra` · _type:_ `task` · _confidence:_ `1.00` · _model:_ `?` · _cost:_ `$0.0000`

> The convocation just scanned its own repository and surfaced the following observations (severity 1 = minor, 5 = urgent):
>
>   - [todo sev=4] umbris-core/tests/test_introspection.py:102: XXX: urgent rewrite
>   - [stale_link sev=3] lore/revolutions/2026-05-26_opening-the-convocation.md: broken link → "../../brand/profile.png"
>   - [test_gap sev=3] umbris-core/src/umbris/agents/base.py: module `base` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/iuppiter.py: module `iuppiter` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/luna.py: module `luna` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/mars.py: module `mars` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/mercurius.py: module `mercurius` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/saturnus.py: module `saturnus` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/sol.py: module `sol` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/stella.py: module `stella` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/agents/venus.py: module `venus` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/cli.py: module `cli` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/compositione.py: module `compositione` has no matching test file
>   - [test_gap sev=3] umbris-core/src/umbris/d…

### ☿ Mercurius · the swift · says:

_agent:_ `mercurius_00` · _type:_ `observation` · _confidence:_ `0.87` · _model:_ `claude-sonnet-4-6` · _cost:_ `$0.0154`

> _What I notice first · the test gaps are noise. Thirty-plus modules untested is a baseline condition for a young codebase, not a crisis. The signal is `test_introspection.py` itself: the one file that exists to test the system's self-knowledge is the file carrying a sev=4 urgent rewrite, two critical FIXMEs, a HACK, and a broken edge case · all clustered in roughly 200 lines. The introspection module is the convocation's eye turned inward. If that eye is compromised, the scan that produced this entire list cannot be trusted._

_structured payload:_

```json
{
  "observation_type": "constraint",
  "target": "umbris-core/tests/test_introspection.py",
  "finding": "The single existing test file is also the most structurally compromised artefact in the scan. It carries a sev=4 urgent rewrite marker (line 102), two FIXME:critical markers (lines 284 and 93), a HACK workaround (line 101), and a broken edge case (line 83). This is the module responsible for validating the system's introspective accuracy · the very mechanism that generated the current observation list. Unreliable introspection tests mean the severity ratings and gap detections surfaced here are of unknown reliability. Fixing `test_introspection.py` is a prerequisite for trusting any subsequent scan output, and therefore a prerequisite for rationally prioritising the remaining 33 test gaps.",
  "recommended_first_change": "Audit lines 83-102 and 284 of test_introspection.py. Resolve or document the HACK at line 101 before touching anything else. The sev=4 rewrite at line 102 should be the first pull request opened."
}
```

### ☿ Mercurius · the swift · says:

_agent:_ `mercurius_01` · _type:_ `observation` · _confidence:_ `0.87` · _model:_ `claude-sonnet-4-6` · _cost:_ `$0.0163`

> _What I notice first · the test file that does exist, `test_introspection.py`, is the one riddled with FIXMEs and a sev=4 urgent rewrite. Thirty-three modules have no tests at all, but the single test file you have is already confessed as broken. Fix the foundation before you build the cathedral._

_structured payload:_

```json
{
  "observation_type": "constraint",
  "target": "umbris-core/tests/test_introspection.py",
  "insight": "The repository has exactly one substantive test file and it is self-declared broken at four separate locations, including a sev=4 urgent rewrite at line 102. The thirty-three test_gap items are serious, but they represent absence. This file represents presence of something actively misleading: a test suite that signals coverage while internally flagging critical failure. Any CI signal it produces is noise at best, false confidence at worst. The highest-leverage first move is not to write new tests but to triage test_introspection.py lines 83, 93, 101, 102, and 284 · determine whether they are salvageable or should be deleted. A deleted broken test is more honest than a passing broken test.",
  "recommended_first_change": "Open test_introspection.py. Read lines 83, 93, 101-102, and 284 in sequence. For each: if the underlying behaviour it tests is still the intended behaviour, fix the implementation. If the test itself is the wrong test, delete it. Commit with a message that states plainly what was removed and why. Only after the file is clean does the test_gap work have a coh…
```

### ☿ Mercurius · the swift · says:

_agent:_ `mercurius_02` · _type:_ `observation` · _confidence:_ `0.82` · _model:_ `claude-sonnet-4-6` · _cost:_ `$0.0147`

> _What I notice first · the test gaps are noise masking signal. Thirty-plus modules untested is a single systemic condition, not thirty separate problems. The one move that changes the shape of everything: fix the sev=4 item in test_introspection.py at line 102, because that file is the only test infrastructure the project currently has · and it is already broken from the inside._

_structured payload:_

```json
{
  "observation_type": "priority_reframe",
  "finding": "The mass of sev=3 test_gap items is structurally misleading. They represent the same root condition: the project has not yet built out its test suite. They cannot be addressed individually with high leverage. The single highest-conviction move is resolving the sev=4 'XXX: urgent rewrite' at test_introspection.py:102, because test_introspection.py appears to be the primary existing test file for the entire codebase. A broken or compromised introspection test undermines the validity of any future test work, including the creation of the thirty missing test files. The concrete first change: read test_introspection.py lines 83-102 in full, understand what the urgent rewrite flag is marking, and replace whatever temporary construct sits there with a correct implementation. Everything else is downstream of that file being trustworthy.",
  "leverage_rationale": "Fix the foundation before building on it. Adding thirty test files against a broken introspection harness compounds the error.",
  "target": "umbris-core/tests/test_introspection.py:102",
  "severity": 4
}
```

### ♀ Venus · the gatherer of harmony · says:

_agent:_ `venus_r01_00` · _type:_ `hypothesis` · _confidence:_ `0.78` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0745`

> _Three things knit together here · the sev=4 rewrite in test_introspection.py, the nine missing agent tests (one per planet), and the undocumented `think` on agents/base.py:105. They are the same wound seen from three angles · the convocation has no enforced contract for what an agent is. Fix the contract in one place and three severities collapse together._

_structured payload:_

```json
{
  "hypothesis": "The highest-leverage single move is not to repair test_introspection.py in isolation, but to use the rewrite as the occasion to define the agent contract once and assert it everywhere. Concretely: (1) write the docstring for `think` in umbris-core/src/umbris/agents/base.py:105 as a precise behavioural specification · inputs, outputs, side-effect envelope, provenance obligations; (2) rewrite test_introspection.py around that contract as a parametrized suite that loads each of the nine planet modules (luna, mercurius, venus, mars, sol, iuppiter, saturnus, stella, plus base) and asserts the contract holds; (3) the four FIXMEs and the XXX at lines 83, 93, 101-102, 284 are then either satisfied by the new contract tests or deleted as obsolete with explicit commit notes.",
  "why_it_matters": "Mercurius_00, _01, _02 all correctly identified test_introspection.py as the foundation. The distinctive observation is that nine of the thirty-three test_gaps are not independent debts · they are instances of one missing abstraction. A parametrized introspection harness keyed on the agent contract closes the sev=4 todo, closes nine sev=3 test_gaps, and closes the sev=2 missing…
```

### ♀ Venus · the gatherer of harmony · says:

_agent:_ `venus_r01_01` · _type:_ `hypothesis` · _confidence:_ `0.68` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0709`

> _Three things knit together here · the broken introspection test, the thirty-three absent test files, and the FIXME at line 93 that says severity ordering itself is wrong. They are one wound, not three. The introspection module is the organ that detects gaps · repair it so that the same scan emits the scaffolds for what is missing, and the cathedral begins to raise its own walls._

_structured payload:_

```json
{
  "hypothesis": "The highest-leverage single move is to treat test_introspection.py:102 not as a bug fix but as the seed of a self-healing pattern: repair the urgent rewrite AND extend the introspection logic so that, when it detects a test_gap, it emits a minimal test stub file at the canonical path (tests/test_<module>.py) with the correct import, a skipped placeholder test, and a docstring naming the source module. One pull request closes the sev=4, restores trust in the introspection harness, and converts the 33 sev=3 test_gaps from a manual backlog into a generated scaffold the convocation can fill in iteratively.",
  "why_distinctive": "Other planets will correctly converge on 'fix test_introspection.py first' · this is the Mercurius consensus already on the blackboard. The integrative move they are least likely to take is to fuse the foundation fix with the gap-closure mechanism, so that the act of repairing the eye also gives the eye hands. The FIXME at line 93 ('should outrank TODO in severity') hints that the introspection module already reasons about severity ordering · the same module is the natural home for emitting remediation artefacts.",
  "concrete_first_change"…
```

### ♂ Mars · the challenger · says:

_agent:_ `mars_r01_00` · _type:_ `critique` · _confidence:_ `0.78` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0851`

> _The convocation has agreed too smoothly. Press here · Venus_r01_00 (c8e86cb0) promises one PR collapses a sev=4, nine sev=3 test_gaps, and a sev=2 docstring · but the entire bundle rests on a guess that test_introspection.py:83-102 actually exercises `think` on agents/base.py. Nobody has read the file. Venus_r01_01 implicitly contradicts this by treating introspection as the gap-detection scanner, not an agent-contract harness. Both Venuses cannot be right, and Mercurius x3 never specified which._

_structured payload:_

```json
{
  "attacked_hypothesis": "venus/venus_r01_00 (c8e86cb0)",
  "failure_mode": "Unverified coupling. The hypothesis claims a single pull request closes the sev=4 rewrite, nine missing-agent-test sev=3s, and the missing docstring on `think` · but this leverage exists only if test_introspection.py:83-102 is in fact about agent behaviour rather than substrate scanning. Venus_r01_00's own falsifiable_prediction concedes the framing collapses if the file is about scanning. Venus_r01_01, on the same blackboard, treats the file as a gap-detection module that 'already reasons about severity ordering' · a scanner, not an agent harness. The convocation is recommending a rewrite scoped to an interpretation that a co-author of the consensus already disagrees with. Worse: every Mercurius observation asserts 'fix this file first' without naming what the file tests. The whole stack of priorities rests on an unread file.",
  "secondary_smoothness_concern": "The convocation has uniformly dismissed the 33 test_gaps as 'noise' or 'one systemic condition.' This is rhetorically convenient but analytically lazy. daemon/safety.py, daemon/apply.py, provenance.py, and the llm/providers/* are not interchang…
```

### ♂ Mars · the challenger · says:

_agent:_ `mars_r01_01` · _type:_ `critique` · _confidence:_ `0.78` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0804`

> _Press here · Venus_r01_01 wants the introspection module to auto-emit thirty-three skipped test stubs. That is the exact pathology Mercurius_01 named one hypothesis earlier · presence of something actively misleading is worse than honest absence. A directory of green-passing skip markers is false coverage with provenance, and it will outlive whoever wrote it. Unanswered · how does scaffold_missing_tests avoid becoming the next sev=4 rewrite three months from now?_

_structured payload:_

```json
{
  "target_hypothesis": "venus/venus_r01_01 (d43b7fb4) · scaffold_missing_tests fusion move",
  "weakness_type": "internal_contradiction_with_cited_foundation",
  "finding": "Venus_r01_01 explicitly builds on the Mercurius consensus, but its concrete proposal violates the principle that consensus rests on. Mercurius_01 stated plainly: 'A deleted broken test is more honest than a passing broken test.' Venus_r01_01's PR #2 generates 33 pytest stub files containing skipped placeholder tests at canonical paths. Skipped tests in a CI pipeline are functionally indistinguishable from passing tests for most dashboards and aggregate counters · they convert 33 visible sev=3 test_gap observations into 33 invisible skip markers buried inside files that now look like coverage. The introspection scanner that surfaced the gaps will, after this PR, find matching test files for every module and stop reporting the gap. The debt is laundered, not paid.",
  "concrete_failure_mode": "After PR #2 lands, the next run of the introspection scan no longer surfaces the 33 test_gap items, because tests/test_<module>.py exists for each. The backlog disappears from the convocation's view while the actual cove…
```

### ☉ Sol · the radiant · says:

_agent:_ `sol_r01_00` · _type:_ `synthesis` · _confidence:_ `0.78` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0836`

```json
{
  "answer": "Drawing the threads · the convocation has converged, with one principled objection, on a single highest-conviction next move: repair test_introspection.py before anything else. Mercurius (three independent passes) located the signal under the noise · thirty-three absent test files are one systemic condition, while the single existing test file carries a sev=4 urgent rewrite, two FIXME:critical markers, a HACK, and a broken edge case in roughly two hundred lines. Because that file is the very mechanism that produced this observation list, every downstream priority inherits its unreliability. Venus knit two extensions onto that foundation · a contract-based parametrized harness keyed on a freshly-written docstring for `think` in agents/base.py (venus_r01_00), and a self-healing scaffold that auto-emits stubs for every detected gap (venus_r01_01). Mars broke both extensions cleanly · venus_r01_00 rests on an unread assumption about what test_introspection.py:83-102 actually exercises (agent behaviour vs. substrate scanning), and venus_r01_01 would launder visible debt into thirty-three skip-marked stubs that read as coverage to any future scan, contradicting Mercurius_01's own principle that honest absence beats misleading presence.\n\nHeld together, the highest-conviction next move is this · open test_introspection.py and read lines 83, 93, 101-102, and 284 in order, then publish in the commit message what the file actually imports and asserts (agent contract, s…
```

### ♄ Saturnus · the falsifier · says:

_agent:_ `saturnus_00` · _type:_ `verdict.accepted` · _confidence:_ `0.83` · _model:_ `claude-opus-4-7` · _cost:_ `$0.0743`

> I attempt to falsify · The strongest counter I can mount is that the query asked for the highest-conviction next moves and the candidate answers with a meta-move (read the file) rather than a code change. But the candidate anticipates this exactly in its limitations section and offers an explicit fallback (land the sev=4 at line 102, scope nothing else), and the meta-move is substantively justified by Mars's unrefuted objection that every bundling hypothesis rests on an unread file. Second attack · the candidate claims the sev=4 at line 102 and sev=3 at line 93 are 'coupled' such that one cannot be resolved without the other; this is presented as a falsifiable prediction rather than a load-bearing claim, so even if false it does not break the verdict, only refines PR scope. Third attack · the candidate honours all blackboard constraints: it does not contradict any Mercurius pass, it correctly characterises both Venus hypotheses and both Mars critiques, it does not launder debt, it does not bundle pre-read. Fourth attack · the query said 'which 1' move; the candidate delivers exactly one move (the gated read-then-narrow-fix sequence treated as a single atomic act with a fallback). Acceptable. The synthesis survives.

### ⬤ Umbra · the substrate · says:

_agent:_ `umbra` · _type:_ `provenance.summary` · _confidence:_ `1.00` · _model:_ `?` · _cost:_ `$0.0000`

```json
{
  "query_id": "e47bbe36-ddf3-4fcc-9e66-e6a63be6dd93",
  "started_at": "2026-05-29T15:25:29.122341+00:00",
  "finished_at": "2026-05-29T15:27:27.851616+00:00",
  "wall_seconds": 118.729275,
  "total_records": 10,
  "records_by_role": {
    "umbra": 1,
    "mercurius": 3,
    "venus": 2,
    "mars": 2,
    "sol": 1,
    "saturnus": 1
  },
  "records_by_type": {
    "task": 1,
    "observation": 3,
    "hypothesis": 2,
    "critique": 2,
    "synthesis": 1,
    "verdict.accepted": 1
  },
  "total_cost_usd": 0.515196,
  "cost_by_model": {
    "claude-sonnet-4-6": 0.046422,
    "claude-opus-4-7": 0.468774
  },
  "pricing_as_of": "2026-05"
}
```

## § 2 · The verdict

> Drawing the threads · the convocation has converged, with one principled objection, on a single highest-conviction next move: repair test_introspection.py before anything else. Mercurius (three independent passes) located the signal under the noise · thirty-three absent test files are one systemic condition, while the single existing test file carries a sev=4 urgent rewrite, two FIXME:critical markers, a HACK, and a broken edge case in roughly two hundred lines. Because that file is the very mechanism that produced this observation list, every downstream priority inherits its unreliability. Venus knit two extensions onto that foundation · a contract-based parametrized harness keyed on a freshly-written docstring for `think` in agents/base.py (venus_r01_00), and a self-healing scaffold that auto-emits stubs for every detected gap (venus_r01_01). Mars broke both extensions cleanly · venus_r01_00 rests on an unread assumption about what test_introspection.py:83-102 actually exercises (agent behaviour vs. substrate scanning), and venus_r01_01 would launder visible debt into thirty-three skip-marked stubs that read as coverage to any future scan, contradicting Mercurius_01's own principle that honest absence beats misleading presence.
> 
> Held together, the highest-conviction next move is this · open test_introspection.py and read lines 83, 93, 101-102, and 284 in order, then publish in the commit message what the file actually imports and asserts (agent contract, substrate scanner, or both). That read is the gating act. Conditional on what it reveals, the first pull request does exactly one thing: resolve the sev=4 rewrite at line 102 and the coupled sev=3 FIXME at line 93 (severity ordering), deleting any test whose underlying behaviour is no longer intended rather than patching it green. No scaffolding. No bundled docstring work. No thirty-three stubs. The deliverable is a test_introspection.py whose every remaining assertion is one a human would defend out loud, plus a one-paragraph note in the PR description stating which of the two Venus extensions the file's actual contents licence as the legitimate follow-up. That note is what unblocks PR #2 · either venus_r01_00's contract harness (if the file proves to be about agent behaviour) or a narrower scanner-hardening pass (if it proves to be about substrate introspection). Confidence that this is the right first move: high. Confidence about which Venus extension follows: deliberately deferred to the read.
> 
> Falsifiable predictions for Saturnus · (1) test_introspection.py exists at the stated path and contains the five markers at lines 83, 93, 101-102, 284 as reported; (2) the markers are mutually coupled enough that resolving the sev=4 at line 102 cannot be done cleanly without also touching the sev=3 at line 93; (3) no single PR can honestly close the sev=4 plus the nine agent test_gaps plus the `think` docstring without first establishing, by reading the file, which planet's framing is correct · therefore any synthesis that bundles them pre-read is premature; (4) the 33 test_gaps are not uniform · daemon/safety.py, daemon/apply.py, provenance.py, and llm/providers/* are load-bearing in a way examples/* and console/* are not, and Mars is right that bundling them was a category error this synthesis does not repeat but also does not yet resolve.

## § 3 · The proposed patch

**Patcher one-liner:** Log convocation verdict prioritizing the read-then-repair of test_introspection.py before any scaffolding or bundled work.

### `lore/convocation-decisions/0007-read-before-repair-test-introspection.md`

```
# Convocation Decision 0007 · Read Before Repair

*Filed by the Patcher, on behalf of the convened planets.*

## The drawn thread

Mercurius, across three independent passes, located the signal under the noise · thirty-three absent test files form one systemic condition, while the single existing test file, `test_introspection.py`, carries a sev=4 urgent rewrite, two `FIXME:critical` markers, a `HACK`, and a broken edge case across roughly two hundred lines. Because that file is the very mechanism that produced the observation list, every downstream priority inherits its unreliability.

Venus extended the foundation twice:

- **venus_r01_00** · a contract-based parametrized harness keyed on a freshly-written docstring for `think` in `agents/base.py`.
- **venus_r01_01** · a self-healing scaffold that auto-emits stubs for every detected gap.

Mars broke both extensions cleanly:

- venus_r01_00 rests on an unread assumption about what `test_introspection.py:83-102` actually exercises · agent behaviour, substrate scanning, or both.
- venus_r01_01 would launder visible debt into thirty-three skip-marked stubs that read as coverage to any future scan, contradicting Mercurius_01's own pr…
```

## § 4 · Outcome

- **Status:** `ok`
- **Reason:** convocation shipped
- **Files written:**
  - `lore/convocation-decisions/0007-read-before-repair-test-introspection.md`

---

_This transcript was written automatically by the Custos sentinel · `umbris-core/src/umbris/daemon/transcript.py`._
_The live, animated version lives at [https://umbrisai.com/convocation](https://umbrisai.com/convocation)._
